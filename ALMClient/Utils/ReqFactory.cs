using System.Collections.Generic;

namespace ALMClient.Utils
{
    public class ReqFactory
    {
        //Generate #amount of requirements
        /*public List<Entity> GenerateReq(int ammount)
        {
            List <Entity> reqCollection = new List<Entity>();
            for (int i = 0; i < ammount; i++)
            {
                reqCollection.Add(GenerateReq());
            }

            return reqCollection;
        }

        public Entity GenerateReq()
        {
            Entity req = new Entity();
            return req;
        }*/
    }
}
